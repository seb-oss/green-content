{
  "title": "Design Tokens: From Figma Variables to CSS Custom Properties",
  "slug": "design-tokens-deep-dive",
  "published": true,
  "pinned": false,
  "cover": {
    "type": "image",
    "src": "/assets/primitives_tokens.png"
  },
  "tags": [
    "design"
  ],
  "authors": [
    "jessi",
    "marwin"
  ],
  "content": "## Why tokens matter\n\nDesign tokens are the single source of truth that connects the decisions made in Figma with the values rendered in production. A token like `color.brand.primary` carries meaning — it says 'this is our primary brand colour' — rather than encoding a specific hex value that could change over time.\n\n### Our token pipeline\n\nIn the Green Design System, tokens originate as Figma variables. A custom export plugin transforms them into a structured JSON format, which is then processed by our token build pipeline into CSS custom properties, TypeScript constants, and Tailwind configuration values.\n\n### Semantic vs. primitive\n\nWe distinguish between primitive tokens (raw values like `#0092E1`) and semantic tokens (purpose-driven references like `--gds-color-brand-01`). Components always consume semantic tokens, never primitives. This ensures that a full theme switch can be achieved by swapping a single token set at the root level.\n\n### Dark mode\n\nGreen supports both light and dark themes through the `gds-theme` component. Wrapping any subtree in `<gds-theme color-scheme=\"dark\">` automatically inverts all semantic colour tokens to their dark-mode counterparts.",
  "createdAt": "2026-02-14",
  "updatedAt": "2026-02-14"
}