# .github/workflows/generate-og.yml
name: 🎨 Generate OG Images

on:
  workflow_dispatch: {}

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "og-images"
  cancel-in-progress: true

jobs:
  generate-and-deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "lts/*"

      - name: Install dependencies
        run: |
          npm install canvas

      - name: Process content files
        run: node scripts/content.script.js

      - name: Generate OG images
        run: node scripts/og.script.js

      - name: Verify OG images
        run: |
          if [ -z "$(find generated -name '*.og.png')" ]; then
            echo "No OG images were generated"
            exit 1
          fi
          echo "Found OG images:"
          find generated -name '*.og.png'

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: generated

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

      - name: Commit generated OG images
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add generated
          git status
          git commit -m "Update OG images" || echo "No changes to commit"
          git push
